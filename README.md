# oracle-gdd-sphere-dataset-loader
Toolkit for loading the Meta Sphere dataset (900M+ rows) into Oracle Globally Distributed Database (26ai). Contains SQL scripts for global schema setup, sharded external table configuration, and optimized hybrid parallel loading.
